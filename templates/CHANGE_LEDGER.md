---
doc: CHANGE_LEDGER
tier: standard+   # Light tier exempt; where git exists, `specgraph --history` prefers git -S
version: 0.0.0
status: template
owner: orchestrator
last_updated: —
---

# Change Ledger

_One line per **minor or major** version bump of any doc — patch bumps
exempt. Append-only. This is the no-git fallback for "what changed";
with git present, `git log -S<ID>` is richer and this ledger is optional
(record the choice in D-001)._

| Date | Doc | Version | What Changed (one line) | Decision Ref |
|---|---|---|---|---|
